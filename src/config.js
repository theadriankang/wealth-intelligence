/** One-line switches. Change ADAPTER here on Friday and nothing else moves. */
export const CONFIG = {
  ADAPTER: "juliusbaer",        // "demo" | "juliusbaer" | "csv"
  OFFLINE: false,               // true = never hit the network, fixtures only
  POLL_MS: 60000,
  ASOF: "04 Sep 2026, 08:40 SGT",
  DEMO_BANNER: true             // show the "fabricated data" strip
};
